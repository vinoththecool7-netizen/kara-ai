# Database
DATABASE_URL=postgresql+asyncpg://kara:kara@db:5432/kara

# LLM Configuration (not required until Day 29+)
LLM_API_KEY=
LLM_MODEL=gpt-4o
LLM_PROVIDER=openai

# CORS (JSON array of allowed origins)
CORS_ORIGINS=["http://localhost:3000"]

# Debug mode
DEBUG=false
